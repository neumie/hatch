---
description: Run the hatch AI init guide to bootstrap a new project's hatch config (docker, migrations, dev servers).
disable-model-invocation: true
allowed-tools: [Bash, Read, Write, Edit, Glob, Grep]
---

Read the hatch AI init guide and follow its instructions:

```bash
cat ~/.hatch/prompts/hatch-init.md
```
