---
description: Run the Contember-specific hatch init guide to bootstrap a Contember project's hatch config.
disable-model-invocation: true
allowed-tools: [Bash, Read, Write, Edit, Glob, Grep]
---

Read the Contember-specific hatch init guide and follow its instructions:

```bash
cat ~/.hatch/prompts/hatch-init-contember.md
```
